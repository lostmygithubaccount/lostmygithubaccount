# lostmygithubaccount

Meta-repo for personal projects under the `lostmygithubaccount` GitHub account.

## Repos

| Directory | Purpose |
|-----------|---------|
| `bin/` | Personal CLI scripts |
| `dkdc.net/` | Cody's personal website |
| `dotfiles/` | Dotfiles |

## Setup

```bash
bin/setup  # clones repos
```

## Conventions

- `AGENTS.md` is source of truth; `CLAUDE.md` is always a symlink to it.
